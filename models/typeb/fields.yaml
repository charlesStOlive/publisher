# ===================================
#  Form Field Definitions
# ===================================

fields:
    name:
        label: waka.publisher::lang.typeb.name
    slug:
        label: waka.utils::lang.global.slug
        preset: name
