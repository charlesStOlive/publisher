# ===================================
#  Form Field Definitions
# ===================================

fields:
    name:
        label: waka.publisher::lang.objtext.name
    slug:
        label: waka.utils::lang.global.slug
        preset: name
